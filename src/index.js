/**
 * Public entry point for @alexify/wrpc.
 *
 * TODO: implement the WebSocket RPC protocol (client/server, framing,
 * request/response + streaming, error handling) and export it here,
 * following the same barrel pattern as @alexify/kerberos's src/index.js.
 */
module.exports = {};
